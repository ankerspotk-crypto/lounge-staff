# TRUST 日報（日報登録 / Report）仕様

採取日 2026-08-28。実画面 `admin.trust-operation.com/report/index/2026/08/27`（ラウンジ家康・実データ）を直読み。
※日付指定は **パスセグメント形式のみ**（`/report/index/YYYY/MM/DD`）。クエリ文字列は無視される（[[reference_trust_outsider_scrape]] と同じ罠）。

## 画面の構成（上から）
1. 見出し「2026年08月27日」＋ 年/月/日セレクト＋検索
2. **キャストの表**（`table#lock_box_table` ＝ 人選択、`table#timecard`（1つ目）＝ 数値）
3. **スタッフ（黒服）の表**（`table#lock_box_table_staff` ＋ `table#timecard`（2つ目））
4. **入金**（`table#logCostIn`）
5. **出金＝店の経費**（`table#logCostOut`）
6. **メモ**（自由記述・1日1本）＋ 登録ボタン

⚠️ `table#timecard` は **id が2つ重複**している（キャスト用・スタッフ用）。`querySelectorAll` で2つ取る前提。
⚠️ 人の名前は `timecard` 側ではなく **隣の `lock_box_table` の `select[name=girlId]` の選択中option**。行の並びで対応付ける。

## キャスト表の列（全27列）

| 群 | 列 | 中身 | 入力/計算 |
|---|---|---|---|
| 勤怠 | 開始 | `input[name=startedAt]` `20:30` | 入力 |
| 勤怠 | 終了 | `input[name=finishedAt]` `00:00` | 入力 |
| 勤怠 | 時間外 | `input[name=timeAdjustment]`（**分**） | 入力 |
| 勤怠 | 労働時間 | `3時間30分` | **計算** |
| 勤怠 | 時間給与単価 | 上段=時間報酬 `¥26,250` ／ 下段=単価 `¥7,500`（`input[type=hidden][name=hourly_pay]`） | 単価はマスタ、報酬は計算 |
| 日払い支給額 | 支給額合計 | `¥27,250` | **計算** |
| 日払い支給額 | 残り支給額 | `¥27,250` | **計算** |
| 日払い支給額 | **日払い** | `input[name=1]` | 入力 |
| マイナス | 送り代 | `input[name=5]` | 入力 |
| マイナス | 個人支払い | `input[name=10]` | 入力 |
| マイナス | 宿泊代 | `input[name=11]` | 入力 |
| マイナス | 早上がり | `input[name=14]` | 入力 |
| マイナス | 計 | `¥0` | **計算** |
| バック | 担当小計 %-小計額 | `¥0 0% - ¥59,250` | **売上から自動** |
| バック | 同伴小計 %-小計額 | `¥0 0% - ¥17,500` | **売上から自動** |
| バック | 担当 回数-金額 | `2 回 / --` | **売上から自動** |
| バック | 予約 回数-金額 | `2 回 / ¥1,000` | **売上から自動** |
| バック | 同伴 回数-金額 | `1 回 / ¥3,000` | **売上から自動** |
| バック | ドリンク 杯数-金額 | `1杯 - ¥3,000 / --` | **売上から自動** |
| バック | ボトル 本数-金額 | `--` | **売上から自動** |
| バック | フード 回数-金額 | `--` | **売上から自動** |
| バック | 計 | `¥1,000` | **計算** |
| ボーナス | 送迎手当 | `input[name=12]` | 入力 |
| ボーナス | 残業代 | `input[name=13]` | 入力 |
| ボーナス | 売り半 | `input[name=15]` | 入力 |
| ボーナス | 運営手当 | `input[name=16]` | 入力 |
| ボーナス | 計 | `¥0` | **計算** |

### スタッフ（黒服）表＝キャスト表から **時間給与単価とバック9列を落とした15列**
勤怠4（開始/終了/時間外/労働時間）＋ 日払い（`name=1`）＋ マイナス5 ＋ ボーナス5。
＝ 黒服は「時給×時間」の自動計算もバックも TRUST 側では持っていない。

### 1人2行
各人に **空の追加行**（`削除`リンク付き）が必ず1本ぶら下がる＝同じ人の2本目の勤怠を足すための行。
⚠️ OCR／パースでは **1人1件にまとめる**（過去に2行に割れた事故あり）。

## 計算式（実データで検算済み）
```
労働時間  = (終了 − 開始) + 時間外分     ※終了 < 開始なら翌日跨ぎ
時間報酬  = round(単価 × 労働時間 / 60分)   ← 分あんぶん・四捨五入
   例 3時間30分 × ¥7,500 = ¥26,250 ／ 3時間35分 × ¥5,000 = ¥17,917（215/60×5000=17,916.67）
支給額合計 = 時間報酬 + バック計 + ボーナス計
   例 ¥26,250 + ¥1,000 = ¥27,250
残り支給額 = 支給額合計 − 日払い − マイナス計
   例 ¥13,650 − ¥10,000 = ¥3,650
マイナス計 = 送り代 + 個人支払い + 宿泊代 + 早上がり
ボーナス計 = 送迎手当 + 残業代 + 売り半 + 運営手当
```
⚠️ **マイナス計は 0以上の正の整数**（差し引く額を正で持つ）。

### いえやすの実バック設定（2026-08-27 実データから逆算）
担当小計 **0%** ／ 同伴小計 **0%** ／ 担当 **¥0/回** ／ 予約 **¥500/回** ／ 同伴 **¥3,000/回** ／ ドリンク・ボトル・フード **設定なし**。
＝ いまバックとして立っているのは **予約(場内)¥500 と 同伴¥3,000 だけ**。

## 入金 / 出金
| 表 | 列 | 実体 |
|---|---|---|
| `#logCostIn` | 入金 / 金額 / メモ | `select[name=cost_id]` ＋ `input[name=price]` ＋ `textarea[name=memo]` |
| `#logCostOut` | 出金 / 金額 / メモ | 同上 |

- **入金カテゴリ（1件）**：レジ金入金
- **出金カテゴリ（11件）**：5階 備品 ／ 買い出し ／ 体験 ヘアセット代 ／ 5階 立替分 出前代,たばこ代 ／ 5階 酒類仕入れ ／ 2階 備品 ／ 2階 立替分 出前代,たばこ代 ／ 2階 酒類仕入れ ／ 全体仕入れ ／ 全体経費 ／ 交通費、サロン代
- 実例：`5階 備品 / ¥2,018 / ガス代`、`全体経費 / ¥176,000 / 送り代２２日間分`

## メモ
1日1本の自由記述。実例「11,857円涼介７月給料作成済み」＝ **給与の申し送りが実質ここに溜まっている**。

## 既存コードとの接点
- `parseTrustDailyReport_`（Code.gs）＝ この画面の `#timecard input[name="1"]` 合計と `#logCostOut` を regex で抜くパーサ。**合計しか取っていない**。
- `extractTrustReportWithGemini_`（Code.gs）＝ 日報スクショOCR。**2026-08-27 にボス判断で封印**（`GUNSHI_API_FNS` から除外）。列の取り違えに勝てなかった。
- 名簿は **基本時給・基本バック・バック方式・固定バック率** を既に持つ（`名簿データ辞書.md`）＝ 単価マスタは軍師側に既にある。
