# データ関係 辞書 ＆ 全体地図（マスタ／DBの正本）

> **この資料の役割**：機能追加を重ねて散らかった「どの台帳が正で、誰が誰を見ているか」を一望する**最上位の地図**。
> スタッフ個別の深掘りは姉妹資料 `名簿データ辞書.md`（スタッフマスタ専用）に譲り、こちらは**全ドメインの台帳とその結合**を扱う。
> **運用ルール（ここが本題）**：DB／マスタを触る修正の前に **まずこの資料を見る**。「参照がバラバラ＝都度指摘」を構造的に減らすための入口。
> **確度の凡例**：✅確定（コード直読みで裏取り）／🔶部分確定（主要箇所のみトレース）／⚠️要精査（存在把握・詳細未確認。推測では埋めない）
>
> 最終更新: 2026-07-23（v7：5-2 在庫の死にコード関数7つ撤去・本番@712反映済。v5＝第1歩=中央アクセサ統一@711反映。v4＝給与5-1✅クローズ。v3＝🔴→🟠。v2＝重複マスタ監査。v1＝全体地図）
> 対象: `Code.gs`（本番の鏡・902KB）／罠メモ群（global memory）
> 併読地図（ビジュアル版）: https://claude.ai/code/artifact/5622b518-da13-42fd-877b-fbdc2a8a5e78

---

## 0. 要約

- 土台は **Googleスプレッドシート2冊 ＋ タブ約40枚**。構造そのものはシンプル。✅
- ~~問題は中央の窓口が無いこと~~ → **2026-07-23 解決済み（本番@711）**。`openById` の直叩き **107箇所を中央アクセサに統一**：本体`SpreadsheetApp.openById(SHEET_ID)`×85 → `getOrOpenSS_()`（`Code.gs:509`）／シフト`openById(SHIFT_SHEET_ID)`×22 → **新設 `getShiftSS_()`**（`Code.gs:515`）。以後は新規コードも必ず経由（両ヘルパーに⚠️直書き禁止コメント）。※旧状態＝本体は helper 94／直叩き 85 の半々でバラついていた。✅
- 全システムを貫く**照合キーは2本だけ**：**会員番号**（顧客⇄売上）と **源氏名**（スタッフ⇄シフト/給与/勤怠）。指摘はこの結合点に集中。✅
- **散らかりの震源は7つ**（→ 第4章）。優先度＝高3・中3・低1。
- **重複マスタ（同じ属性を2枚以上が正本保持）は監査で3件**（→ 第5章）。芯だった**給与の手入力額**は🔴→🟠→**✅クローズ**（新優先＋旧レガシー・フォールバックの管理された二層＝無害・2026-07-22実データ確認）。在庫の死にコード関数7つも2026-07-23撤去・本番@712反映済。残る整理は旧シート凍結・命名統一のみ。
- 片付けは**段階式・非破壊**で、全て**テスト環境ファースト**。本番は「本番に移して」の号令が出た時だけ（`feedback_test_env_first`）。

---

## 1. 土台＝2ブックとアクセス方式（✅確定）

| ブック | 定数 | ID（短縮） | 中身 | openById回数 |
|---|---|---|---|---|
| **本体ブック** | `SHEET_ID` | `1dxCjdog…62ASc` | 顧客・予約・スタッフ・給与・売上・勤怠・在庫・現金・連絡・採用…**約39枚** | 85回超 |
| **シフトブック** | `SHIFT_SHEET_ID` | `1cHknHz…QYs2Ns` | **「シフト表」1枚だけ** | 22回 |

- 定義は `Code.gs:18-19`。
- **⚠️シフト業務が2冊に割れている**：`シフト表`＝シフトブック、`シフト申請`＝本体ブック。**同一関数が両ブックを開く**箇所を確認（例 `Code.gs:5132` が `SHIFT_SHEET_ID`／`5153` が `SHEET_ID`）。片方だけ直すと不整合。✅
- **アクセス方式の実態**：各関数が `SpreadsheetApp.openById(...)` を毎回インラインで呼ぶ。中央アクセサ `getOrOpenSS_()` は在るが不採用。→ 第4章①、第5章①。✅

---

## 2. ドメイン別・タブ辞書

> ブック欄が空白＝**本体ブック（SHEET_ID）**。個別トレース済みは✅、未トレースは🔶（ただし本体所属が濃厚）。SSOT＝その情報の正本。

### 顧客
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| お客様管理Y3 | `MASTER_TAB` | 本体✅ | ● | 見出し2行目・更新列が和暦テキスト(R8.7)＝会員切れ誤表示の常習犯（`reference_customer_master_y3`） |
| 予約ログ | `LOG_TAB` | 🔶 | | |

### 予約・席
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| 予約管理 | `YOYAKU_RSRV_TAB` | 本体✅ | ● | 席の唯一の持ち主。ただし実状態は3台帳合成→第4章② |
| 予約リクエスト | `YOYAKU_REQ_TAB` | 🔶 | | |

### スタッフ／キャスト
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| スタッフマスタ | `STAFF_TAB` | 本体✅ | ● | 全システムの源氏名の正本。詳細は `名簿データ辞書.md` |
| 派遣名マッピング | `HAKEN_NAME_MAP_TAB` | 🔶 | | 派遣名→源氏名 |

### シフト ⚠️2ブック分裂
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| シフト表 | `SHIFT_TAB` | **シフト**✅ | ● | 別冊。退職者除外が経路ごとに別実装（`reference_shift_retire_filter`） |
| シフト申請 | `SHIFT_REQUEST_TAB` | 本体✅ | | |
| 当日相談 | `SHIFT_CONSULT_TAB` | 🔶 | | |
| キャストリクエスト | `CAST_REQUEST_TAB` | 本体✅ | | |

### 勤怠
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| 勤怠ログ | `KINTAI_TAB` | 本体✅ | ● | 打刻の一次記録（`project_kurofuku_kintai`） |
| 勤怠ポイント | `KINTAI_PT_TAB_` | 本体✅ | | 月間集計 |

### 給与
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| 給与計算 | `KYUYO_TAB` | 🔶 | ● | 確定ルールは `reference_payroll_rules`。`importPayrollCsv_` は破壊的で使用禁止 |
| 給与手入力 | `KYUYO_MANUAL_TAB` | 🔶 | | 売上調整（担当へ加算/減算） |
| 給与手入力ログ | `KYUYO_ADJ_LOG_TAB` | 🔶 | | |
| 誕生日バック | `BIRTHDAY_BACK_TAB` | 🔶 | | |
| ヘアサロン領収書 | `HAIR_RECEIPT_TAB` | 🔶 | | 立替＝**給与加算**系統（経費立替とは別） |
| 給与受領一覧 | `PAYROLL_RECEIPT_TAB` | 🔶 | | 日払い |
| 日払い記録 | `'日払い記録'`（直書き） | 🔶 | | |

### 売上・来店 ⚠️2パイプライン
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| TRUST報酬 | `TRUST_TAB` | 本体✅ | ● | 月次×キャスト給与。GAS→TRUST直取得は403恒久ブロック（`reference_trust_gas_waf_block`） |
| TRUST取込ログ | `TRUST_IMPORT_LOG_TAB` | 🔶 | | |
| 来店記録 | `VISIT_TAB` | 🔶 | | per-visit（軍師）。売上列はTRUST取込と交わらず埋まらない→第4章③ |
| 売上明細 | `URIAGE_TAB` | 🔶 | | |
| 伝票 | `BILL_TAB` | 🔶 | | |
| 伝票明細 | `BILL_DETAIL_TAB` | 🔶 | | |

### 在庫・発注
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| 在庫発注マスタ | `STOCK_MASTER_TAB` | 本体✅ | ● | 発注点の正本（`project_stock_reorder_point`）。判定式 `在庫数≦発注点` |
| 在庫発注マスタ_旧 | `STOCK_RETIRE_TAB_` | 🔶 | | 🗑️化石 |
| 在庫管理 | `INVENTORY_TAB` | 本体✅ | | |
| 在庫ログ | `INVENTORY_LOG_TAB` | 🔶 | | 動くたび追記 |
| 発注品目マスタ | `ORDER_MASTER_TAB` | 本体✅ | | |
| 発注ログ | `ORDER_LOG_TAB` | 🔶 | | |
| 購入履歴ログ | `PURCHASE_LOG_TAB` | 🔶 | | |
| 棚卸しログ | `STOCKTAKE_LOG_TAB` | 🔶 | | |
| 店舗メニュー | `MENU_MASTER_TAB` | 🔶 | | メニュー連動はボトルのみ（`project_menu_drop_status`） |
| 納品記録 | `'納品記録'`（直書き） | 🔶 | | |

### 現金
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| 現金管理 | `CASH_CHECK_TAB` | 🔶 | ● | 4袋モデル（`project_lounge`） |
| 現金管理_開店 | `OPENING_CHECK_TAB` | 🔶 | | |
| 金庫出金ログ | `SAFE_WITHDRAWAL_TAB` | 🔶 | | |
| 経費立替台帳 | `KEIHI_TATEKAE_TAB` | 🔶 | | 一般経費の立替＝**現金精算**系統（ヘアサロンと別）（`project_keihi_tatekae`） |

### 連絡・記録
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| お知らせ | `NOTICE_TAB` | 🔶 | | 掲示板（`project_notice_board`） |
| お知らせ既読 | `NOTICE_READ_TAB` | 🔶 | | |
| 申し送り | `SHINOKURI_TAB` | 🔶 | | 黒服の日次引き継ぎ（`project_kurofuku_handover`） |
| AI要望 | `IEYASU_REQ_TAB` | 🔶 | | |
| 送迎ログ | `OKURI_TAB` | 🔶 | | |
| アテンドログ | `ATEN_TAB` | 🔶 | | 付け回し |

### 採用
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| 面談表 | `MENDAN_TAB` | 🔶 | ● | 現行（`project_mendan_karte`） |
| 面談カルテ | `'面談カルテ'`（直書き） | 🔶 | | 🗑️旧名の化石 |

### 領収
| タブ | 定数 | ブック | SSOT | 備考 |
|---|---|---|---|---|
| 領収書記録 | `'領収書記録'`（直書き） | 🔶 | | 客向け発行と経費側は向きが逆。取り違え注意（`project_lounge_receipt_issuance`） |

---

## 3. 全体をつなぐ照合キー（背骨・✅確定）

### 会員番号 — 顧客 ⇄ 売上
- **お客様管理Y3** を **来店記録・TRUST報酬・売上明細・伝票** に結ぶ。
- **正は「番号のみ」**（4桁運用・照合は先頭ゼロ無視）。
- **地雷**：名字フォールバック照合が同名別人の売上を混線させた（@685）→ **番号のみ**に修理済（@686）。誤りは読取り時だけで保存は無傷。仕訳タブで付替え可（`reference_visit_attribution_matching`）。

### 源氏名 — スタッフ ⇄ シフト／給与／勤怠
- **スタッフマスタ** を **シフト表・給与計算・勤怠ログ・誕生日バック** に結ぶ。派遣は**派遣名マッピング**経由。
- **地雷**：`normalizeName_` が内部スペースを消さない＝**「鈴木 海」と「鈴木海」が別人**化。照合系は空白除去キーを自前で作る（`reference_name_normalization`）。

---

## 4. 散らかりの震源（参照バラバラの正体・優先度つき）

| 優先 | 震源 | 中身 |
|---|---|---|
| ~~高~~ **済** | ~~中央アクセサ不在~~ **→ 解決(本番@711・2026-07-23)** | 直叩き107箇所を `getOrOpenSS_()`／新設 `getShiftSS_()` に統一。ライブ稼働確認済。→ 第6章 第1段階=完了。✅ |
| **高** | **席＝3台帳合成＋日付定義2種** | 予約管理／RSRV_客名コピー／在席 を毎回合成。客名=営業日・在席=暦日で0〜6時に1日ズレ。「ご来店」誤表示＝台帳破損サイン（`reference_seat_state_ledgers`）。 |
| **高** | **売上が2パイプライン非交差** | 来店記録(per-visit) と TRUST取込(月次×給与) が別系統でUI導線が片方に無く、軍師来店の売上が永久に空（`reference_visit_sales_two_pipelines`）。 |
| 中 | シフト2ブック分裂 | 表=別冊／申請=本体。跨ぐ関数あり。 |
| 中 | 照合キーの不統一 | 会員番号(番号のみ)・源氏名(空白/派遣名)で個別実装が散る。共通ヘルパー未徹底。 |
| 中 | 退職者フィルタ複数経路 | 在籍者を引く経路が画面用と通知用で別実装。除外もれ（`reference_shift_retire_filter`）。 |
| 低 | 旧タブの化石同居 | 在庫発注マスタ_旧／面談カルテ(旧名) 等。命名も `_TAB` 定数と直書き文字列が混在。 |

---

## 5. 重複マスタ監査（同じ属性を複数シートが正本保持）— 2026-07-22

> 「ほぼ同じ属性を数カ所がマスタとして持っていないか」の実コード監査（サブエージェント＋フロント裏取り）。証拠は `Code.gs:行` / `portal.html:行`。

### ✅ 5-1. 給与の手入力額 — 新（`給与手入力`/`ヘアサロン領収書`）優先＋旧（`給与計算`）レガシー・フォールバック 〔確認済み・クローズ〕
> 当初🔴で報告 → **フロント(portal.html)を精読して🟠に格下げ** → **2026-07-22 実データ確認で✅クローズ**。実態は「無秩序な二重持ち」ではなく、**新が正・旧は新が空の月だけ表示するフォールバック**の意図的設計だった。
> **✅ クローズ根拠（2026-07-22）**：ボスが本番「給与計算」シートを実確認 → 直近月の `キャスト紹介料(手入力)` / `ヘアサロン立替(手入力)` に**0以外なし**＝凍結列に悪さするデータは存在しない。フォールバックはレガシー月を守る意図通りの無害動作。**コード変更なしでクローズ**（単純撤去はレガシー月を壊すため、触らないのが正解）。
- **新（現行・正）**：紹介料＝コンソール `adminSetKyuyoManual`(`Code.gs:2763`)→`給与手入力`／ヘアサロン＝`ヘアサロン領収書` 集計(`getHairReceipts_` `2845`)。ポータル給与明細は新`sales`(`mergeTrustSales_` `2926`)で描画。
- **旧（レガシー・フォールバック）**：`portalPay_`(`9269`) が `給与計算` 行を `pay` で返すが、フロントは**新があれば新／無ければ旧**にしか使わない：
  - 新バック `portal.html:1737` ＝ `担当バック>0 ? 新 : p['新バック']`（二重計上回避をコメント明記 `1735-1736`）
  - ヘアサロン `portal.html:1748-1749` ＝ `領収書>0 ? 領収書 : p['ヘアサロン立替(手入力)']`（受領額 `sendPayrollReceipt` も同型 `2006`）
- **旧列は凍結された入力面**：`calcAndWriteKyuyo_`(`12542`) は既存の手入力値を**毎回引き継ぐだけ**で新規書込口なし(`12583-12605`)。`KYUYO_TAB` への setValue は他に存在しない。
- **残るリスク（限定的）**：現行月で新ソースが空(0)なのに旧`給与計算`列に古い非0値が残っていると、その古い値が明細・受領額に混じる。＝systemicな食い違いではなく**データ整合のエッジケース**。
- **⚠️直し方の注意**：フォールバックを単純撤去すると**レガシー月（新システム移行前）のヘアサロン/新バック表示が消える＝壊す**（`reference_payroll_rules` の「意図的挙動を直すな」に該当）。安全策＝①実データで旧列に非0が残る現行月があるか確認 → ②必要ならフォールバックを移行前の月に限定＋旧列の凍結を明示。**本番給与ロジックはテスト隔離不可**（testフロントは本番backend共用）につき、実データ確認が先。
- **判定**：**✅クローズ（無害・対応不要）**。フロント/バック両側をコード直読み＋本番シートを実データ確認済み。将来もし手入力2列に*現行月*の値を入れる運用を始めるなら再評価。

### 🟠 5-2. 在庫マスタ三重化 — `在庫発注マスタ`(正) vs `在庫管理`(旧) vs `発注品目マスタ`(旧)（死にコード・掃除）
同属性（品名・フロア・在庫数・最低在庫数）を3枚が持つが、既に移行済み。
- **正**：`在庫発注マスタ`(STOCK_MASTER) ヘッダー `Code.gs:14569`、`getStockList`(`14633`)・API登録済み・全面稼働。
- **旧**：`在庫管理`(`14349`)／`発注品目マスタ`(`14403`)。`migrateToStockMaster_`(`15442`) が両者を「(旧)」と明記し一回移行。
- 旧の書込関数7つ（`getInventoryList`/`updateInventoryQty`/`addInventoryItem`/`deleteInventoryItem`／`getOrderMasterList`/`addOrderMasterItem`/`deleteOrderMasterItem`）は到達不能な死にコードだった（全ランタイム＋本番HTMLで呼び出し0・GUNSHI_API_FNS未登録を確認）。
- **✅ 2026-07-23 撤去完了・本番@712反映済**：上記7リーフ関数を撤去（56行減・構文検証済・撤去後もポータル`ok:true`でライブ稼働確認）。ヘルパー `getInventorySheet_`（おみやげ在庫が使用中）／`getOrderMasterSheet_`／`getOrderLogSheet_`（発注システムが多用）は**温存**。旧シート自体（`在庫管理`＝おみやげが現用／`発注品目マスタ`＝孤立）は**データなので削除せず放置**。
- **判定**：✅ 正本1つに集約＋呼び出し可能な旧関数を撤去。将来の誤 `google.script.run` 直叩きで旧シートに乖離する穴を塞いだ（[[reference_concurrent_session_overwrite]]と同種のミラー地雷の予防）。

### 🟡 5-3. おみやげ在庫数だけ `在庫管理`、他は `在庫発注マスタ`（意図的分割）
「在庫数」という同一属性が品目で別シートに分かれる。
- おみやげ(SOUVENIR)は `在庫管理` で増減（`decrementSouvenirStock_` `11731` ほか）。他全品目は `在庫発注マスタ`（`changeStockQty` `14670`）。`migrateToStockMaster_` がおみやげを移行対象から除外（`15448`）。
- **判定**：✅ 意図的。ただし同属性が2シートに分かれる紛らわしさは残る（`在庫管理` はおみやげ1用途のみ生存・汎用UI関数は死んでいる）。

### ✅ 重複ではないと確認（正本1本でOK・安心材料）
- **スタッフ給与属性**（時給/バック率/誕生日/入店日/バック方式/固定バック率）＝ `スタッフマスタ`1本（`STAFF_TERM_HEADERS` `8576`, `STAFF_BACKRULE_HEADERS` `1924`）。`給与計算`のバック率は月次スナップショット、`誕生日バック`は週レンジ上書き＝**派生**。
- **顧客（客名・会員番号）**＝ `お客様管理Y3`1本（`getCustomerMasterCols_` `15519`）。`予約管理`/`来店記録`/`伝票` のはイベント時点スナップショット（**派生コピー**）で第2マスタではない。
- **現金**（`現金管理`閉店 vs `現金管理_開店`）＝ 別イベントの台帳。**PIN**(`KIOSK_PIN_<名前>`)・**LINE userId**・**派遣名マッピング**・**電話**（面談表）＝ 複製なし。

---

## 6. 片付けの方針（段階式・非破壊／全てテスト環境ファースト）

| 段階 | 内容 | リスク |
|---|---|---|
| **0** | **この地図をrepoに固定**（本資料）。`名簿データ辞書.md` と同格に。修正前に必ずここを見る運用へ。**← 完了（v1）** | 今すぐ |
| 1 | **✅完了（本番@711・2026-07-23）** 参照を中央アクセサに統一：`getOrOpenSS_()`（本体）／新設 `getShiftSS_()`（シフト）へ直叩き107箇所を機械置換。新規コードは必ず経由（両ヘルパーに⚠️コメント）。 | 済 |
| 2 | **エンティティ別にSSOTを1枚に確定**。5-1（給与）は**✅実データ確認でクローズ済**（対応不要）。残る対象＝席（3台帳）・売上（2パイプライン）・退職フィルタの多重台帳を「読む台帳」1本に単一化。 | 要検証 |
| 3 | **化石の隔離と命名統一**。**✅5-2の旧在庫の孤立関数7つ撤去・本番@712反映済（2026-07-23）**。残＝旧シートの凍結/リネーム・直書き文字列の `_TAB` 定数統一。 | 整理 |

---

## 7. 関連ドキュメント／メモ

- `名簿データ辞書.md` — スタッフマスタ専用の深掘り辞書（列辞書・中間層関数・依存マップ）。
- `CLAUDE.md` — 3層アーキテクチャ・デプロイの真実・コード.jsセクション地図。
- global memory: `reference_gunshi_code_map`（grep前の索引）／各罠メモ（本資料の備考で個別参照）。
- ビジュアル地図: https://claude.ai/code/artifact/5622b518-da13-42fd-877b-fbdc2a8a5e78
